[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "votovivo-data-aggregator"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = []
packages = ["utils"]
